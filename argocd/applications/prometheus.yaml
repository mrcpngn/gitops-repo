apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: prometheus
  namespace: argocd
spec:
  source:
    repoURL: 'https://github.com/mrcpngn/gitops-repo.git'
    targetRevision: main
    path: charts/prometheus
    helm:
      valueFiles:
      - ../../env/prometheus/values.yaml
  destination:
    namespace: monitoring
    server: 'https://kubernetes.default.svc'
  project: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true

